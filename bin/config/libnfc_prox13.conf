# Конфигурация libnfc для PROX 13 MHz Reader через USB CDC (COM-порт)
# Замените COM3 на ваш реальный порт из Диспетчера устройств Windows.

device.name = "PROX 13 MHz Reader"
device.connstring = "prox13:COM4:9600"
